# Name der SWE
swe.name=Segment 4 Datenübernahme und Aufbereitung (DUA), SWE 4.2 Pl-Prüfung logisch UFD

# Identifikator der SWE (Name des Java-Pakets)
swe.id=de.bsvrz.dua.pllogufd

# Version der SWE
swe.version : 1.0.0

# Pfad zu den Quelldateien
src.dir=src

# Benötigten Java-Bibliotheken
classpath=../de.bsvrz.sys.funclib/bin;../de.bsvrz.dav.daf/bin

# Pfad zu benötigten Bibliotheken und Dateien
# Dateien die hier enthalten sein müssen:
# gpl.txt, junit-4.1.jar, LiesMich.txt, MANIFEST.MF
lib.dir=lib

# Ausgabeverzeichnis für erzeugte Dateien
extra.dir=extra

# Ausgabeverzeichnis für erzeugte Dateien
out.dir=release

# Pfad wo die Dokumentation abgelegt werden soll
doc.dir=doc

# Pfad zu zusätzlichen Quellen
extra.src.dir=../de.bsvrz.sys.funclib/src;../de.bsvrz.dav.daf/src

# Pfad zu den JUnit-Quelldateien
junit.dir=junit

# Verzeichnis für temporäre Dateien
tmp.dir=ant/tmp
